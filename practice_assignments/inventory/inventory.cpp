#include<iostream>
#include<cll.h>
#include<item.h>

/*	PROGRAM DESCRIPTION:
 *	An extremely rudimentary inventory system.
 *	Each item has a name, a description, and a unique ID.
 *	You have exactly 8 inventory slots.
 *	The inventory system is a circular linked list.
 */

int main() {
	item testItem(;
	
	return 0;
}
